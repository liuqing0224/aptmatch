{
  "name": "aptmatch-jobseeker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AptMatch 求职端独立项目",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -n job-api,job-web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server",
    "test": "npm run test -w server && npm run test -w web"
  },
  "devDependencies": { "concurrently": "^9.1.0" }
}
